import std.stdio;
import std.string;
import std.conv;
import std.algorithm;
import std.math;

void main() {
	int N = to!int(chomp(readln()));
	
	N.writeln;
	for(int i = 0; i < N; i++) {
		1.writeln;
	}
}
