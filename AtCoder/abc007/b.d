import std.stdio;
import std.string;
import std.conv;
import std.algorithm;
import std.math;

void main() {
	string A = readln.chomp;
	if(A[0] == 'a' && A.length == 1) "-1".writeln;
	else "a".writeln;
}
